# Docker-specific configuration for Phase 1 testing

# Server
server.port=8081
spring.application.name=auth-service

# Database (PostgreSQL)
spring.datasource.url=jdbc:postgresql://postgres-auth:5432/auth_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baselineOnMigrate=true

# Redis
spring.data.redis.host=redis
spring.data.redis.port=6379

# JWT (loaded from env vars by entrypoint.sh)
# These are set as system properties by the entrypoint script
# mhsa.app.jwtPrivateKey
# mhsa.app.jwtPublicKey
mhsa.app.jwtExpirationMs=3600000
mhsa.app.jwtIssuer=mhsa.backend
mhsa.app.jwtAudience=mhsa-api
mhsa.app.jwtSigningKid=mhsa.app.jwtSigningKid
mhsa.app.jwtAllowHs256Fallback=false

# S3 Storage
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.bucket=mhsa-media
s3.access-key=minioadmin
s3.secret-key=minioadmin
s3.path-style-access=true

# Auth domain-event topic exchange (enriched grant events fan out here). Consumers must match it.
mhsa.auth.events.exchange=${AUTH_EVENTS_EXCHANGE:auth.events}

# Therapist-api integration (assignment read-model replica + nightly reconciliation)
therapist.api.exchange=${THERAPIST_API_EXCHANGE:booking.exchange}
therapist.api.base-url=${THERAPIST_API_BASE_URL:http://therapist-api:8085}
therapist.api.assignments-path=${THERAPIST_API_ASSIGNMENTS_PATH:/internal/assignments}
therapist.api.page-size=${THERAPIST_API_PAGE_SIZE:200}

# Management / Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# Logging
logging.level.root=INFO
logging.level.com.mhsa.backend=DEBUG
logging.level.org.springframework.security=DEBUG
